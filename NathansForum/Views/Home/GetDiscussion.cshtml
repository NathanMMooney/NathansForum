@model NathansForum.Models.Discussion

@{
    ViewData["Title"] = "Discussion Details";
}

<h1>Details</h1>


<div class="container-fluid bg-white border border-black">

    <div class="row">
        <div class="col-md-6 ">
            <img src="/photos/@Model.ImageFileName" class="img-fluid shadow-lg rounded-5 mt-3 mb-3" width="750" height="750"/>
        </div>

        <div class="col-md-6 align-content-center">
            <dl class="row">
                <dt class="col-sm-2">
                    @Html.DisplayNameFor(model => model.Title)
                </dt>
                <dd class="col-sm-10">
                    @Html.DisplayFor(model => model.Title)
                </dd>
                <dt class="col-sm-2">
                    @Html.DisplayNameFor(model => model.Content)
                </dt>
                <dd class="col-sm-10">
                    @Html.DisplayFor(model => model.Content)
                </dd>
                <dt class="col-sm-2">
                    @Html.DisplayNameFor(model => model.ImageFileName)
                </dt>
                <dd class="col-sm-10">
                    @Html.DisplayFor(model => model.ImageFileName)
                </dd>
                <dt class="col-sm-2">
                    @Html.DisplayNameFor(model => model.CreateDate)
                </dt>
                <dd class="col-sm-10">
                    @Html.DisplayFor(model => model.CreateDate)
                </dd>
                <dt class="col-sm-2">
                    @Html.DisplayNameFor(model => model.DiscussionId)
                </dt>
                <dd class="col-sm-10">
                    @Html.DisplayFor(model => model.DiscussionId)
                </dd>
            </dl>
        </div>
    </div>
</div>

<a type="button" class="btn btn-light mb-3 mt-3" asp-controller="Comments" asp-action="Create" asp-route-id="@Model.DiscussionId">Add a Comment</a>

@if (Model.Comments != null)
{
    @foreach (var comment in Model.Comments)
    {
        <div class="container-fluid">
            <div class="row mt-1 p-1 bg-white border border-black">
                <div class="col-md-9 align-content-center">
                    Anonymous user said: @comment.Content
                </div>
                <div class="col-md-3">
                    Posted at @comment.CreateDate
                </div>
            </div>
        </div>
        
    }
}